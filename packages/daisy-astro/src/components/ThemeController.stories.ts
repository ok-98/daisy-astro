import ThemeController from './ThemeController.astro';

// Dummy story. One story per doc-page example + one per variant axis is still
// TODO — see plans/README.md §8: https://daisyui.com/components/theme-controller/
export default {
  title: 'Components/ThemeController',
  component: ThemeController,
};

export const Default = {
  args: {},
};
