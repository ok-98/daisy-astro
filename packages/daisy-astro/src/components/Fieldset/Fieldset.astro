---
// Dummy scaffold. Variant props (color/size/style axes), slots and any
// wrapper markup are still TODO — fill in from the doc page per the workflow
// in plans/README.md: https://daisyui.com/components/fieldset/
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'fieldset'> {}

const { class: className, ...rest } = Astro.props;
---

<fieldset class:list={['fieldset', className]} {...rest}>
  <slot />
</fieldset>
