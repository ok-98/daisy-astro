---
// Dummy scaffold. Variant props (color/size/style axes), slots and any
// wrapper markup are still TODO — fill in from the doc page per the workflow
// in plans/README.md: https://daisyui.com/components/badge/
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'span'> {}

const { class: className, ...rest } = Astro.props;
---

<span class:list={['badge', className]} {...rest}>
  <slot />
</span>
