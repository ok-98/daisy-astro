import Range from './Range.astro';

// Dummy story. One story per doc-page example + one per variant axis is still
// TODO — see plans/README.md §8: https://daisyui.com/components/range/
export default {
  title: 'Components/Range',
  component: Range,
};

export const Default = {
  args: {},
};
