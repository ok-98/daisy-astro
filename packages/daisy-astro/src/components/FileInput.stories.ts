import FileInput from './FileInput.astro';

// Dummy story. One story per doc-page example + one per variant axis is still
// TODO — see plans/README.md §8: https://daisyui.com/components/file-input/
export default {
  title: 'Components/FileInput',
  component: FileInput,
};

export const Default = {
  args: {},
};
