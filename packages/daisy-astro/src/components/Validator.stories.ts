import Validator from './Validator.astro';

// Dummy story. One story per doc-page example + one per variant axis is still
// TODO — see plans/README.md §8: https://daisyui.com/components/validator/
export default {
  title: 'Components/Validator',
  component: Validator,
};

export const Default = {
  args: {},
};
